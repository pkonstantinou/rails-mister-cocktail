<div class="banner"
     style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path @cocktail.photo.key if @cocktail.photo.attached? %>);">
  <div class="container">
    <h1><%= @cocktail.name %></h1>
  </div>
</div>
